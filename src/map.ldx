{
include('ldxflib.lua')
if SERVER then 
    AddCSLuaFile() 
end
}

Component TestMenu = {
    {local store = createStore();}
    <DFrame
        id={"root"} size={ScrW(), ScrH()} popup
        pos={0, 0} title={""} draggable={false}>
            {local counter = useState(0)}
            MAP(@counter.value) [
                <DLabel @counter setText={$ITEM.value} />
            ]
            <DLabel @store setText={store.filters} />
    </DFrame>
}